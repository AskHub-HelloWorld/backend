services:
  postgres:
    image: 'pgvector/pgvector:pg16'
    container_name: ask_hub_local
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=ask_hub
    ports:
      - '5432:5432'
    volumes:
      - ./postgres_data:/var/lib/postgresql/data